# This is how we intend to use it 

tk = etk.init() # loads all the extractors

# API methods to be decided 
# Should return all ages in the input string in the specified format
tk.extract_age(input_string)
